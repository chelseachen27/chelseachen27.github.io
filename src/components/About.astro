---
import { about } from "../data";
---

<section id="about" class="mb-10 md:mb-16">
  <h2 class="mb-4 text-xs font-bold uppercase tracking-widest text-gray-400">About</h2>
  <div class="content-card space-y-4 text-gray-600 leading-relaxed">
    {about.map((paragraph) => (
      <p set:html={paragraph} />
    ))}
  </div>
</section>

<style>
  :global(section#about strong) {
    color: var(--color-gray-900);
    font-weight: 600;
  }
</style>
